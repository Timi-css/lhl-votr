DROP TABLE IF EXISTS results CASCADE;

CREATE TABLE results (
  id SERIAL PRIMARY KEY NOT NULL,
  poll_id INTEGER REFERENCES users(id) ON DELETE CASCADE,
  choice VARCHAR(350) NOT NULL,
  rating INTEGER  NOT NULL DEFAULT 0
);

